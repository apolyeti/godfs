syntax = "proto3";
package chat;

option go_package = "chat/";

message Message {
    string user = 1;
    string text = 2;
}

service Chat {
    rpc Join (Message) returns (stream Message) {}
    rpc Send (Message) returns (Message) {}
}